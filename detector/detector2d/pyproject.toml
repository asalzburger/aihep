[project]
name = "detector2d"
version = "0.1.0"
description = "2D detector geometry and trajectory-intersection primitives"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["detector2d"]
